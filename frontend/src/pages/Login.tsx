import React from 'react';
import { Button } from '../components/ui/button';

const LoginPage = () => {
  return (
    <div>
      <h1>Login</h1>
      <Button>Login</Button>
    </div>
  );
};

export default LoginPage;
